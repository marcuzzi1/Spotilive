<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:viewmodels="clr-namespace:spotilive.ViewModels" x:DataType="viewmodels:AlbumsViewModel"
             x:Class="SpotiliveTryHard.Pages.AlbumsPage">
    <ContentPage.Content>
        <ListView
            CachingStrategy="RecycleElement"
          ItemsSource="{Binding ListOfAlbums}"
          RowHeight="70">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Margin="8">
                            <!--<Label Text="{Binding }" />-->
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
</ContentPage>